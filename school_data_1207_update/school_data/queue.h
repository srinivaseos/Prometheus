#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int id_generator();
//char* names_genertor(int queue_id);
//char* names_generator(int queue_id);
char* names_generator();
char* gender_choose();

int attend_generator();


// login.h

int login_details();
int admin(int admin_id);
int stduent(int student_id);


//socket 
int server(); 
